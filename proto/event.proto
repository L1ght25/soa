syntax = "proto3";

package event;

option go_package = "./proto";


enum EventType {
    VIEW = 0;
    LIKE = 1;
}

message Event {
    int32 task_id = 1;
    EventType event_type = 2;
    int32 author_id = 3;
    int32 user_id = 4;
}
